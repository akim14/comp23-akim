# By: Aromie Kim
#! /usr/bin/env python
# ip_addresses.py - Lists all possible ipv4 addresses

import string

for a in range(0, 256):
    for b in range(0, 256):
        for c in range(0, 256):
            for d in range(0, 256):
                print str(a) + "." + str(b) + "." + str(c) + "." + str(d)


